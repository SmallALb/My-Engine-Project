#include "fspcs.h"
#include "FISH/Object/Object.h"
#include "ObjReader.h"

namespace FISH {
    Object3DPtr FISH::ObjReader::readModel(const string& path) {
        //临时先这样
        return nullptr;
    }
    ModelData ObjReader::readModelData(const string &path) {
        //临时先这样
      return ModelData();
    }
}