#include "fspcs.h"
#include "FISHMemory.h"

namespace FISH {
    // void* Alloc(size_t size) {
    // #ifdef _WIN32
    //     return HeapAlloc(GetProcessHeap(), 0, size);
    // #else
    //     return malloc(size);
    // #endif
    
    // }

    // void Free(void* ptr) {
    // #ifdef _WIN32
    //     HeapFree(GetProcessHeap(), 0, ptr);
    // #endif
    //     free(ptr);
        
    // }

}